<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PocketGuru" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:fontFamily">@font/josefin_sans</item>
        <item name="fontFamily">@font/josefin_sans</item>
    </style>

    <style name="Theme.PocketGuru" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/josefin_sans</item>
        <item name="fontFamily">@font/josefin_sans</item>
        <!-- keep all existing theme attributes -->
    </style>
    <!-- Style requested in AndroidManifest.xml -->
    <style name="Theme.PocketGuru.NoActionBar" parent="Theme.PocketGuru" />
</resources>
